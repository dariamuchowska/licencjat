{% include 'default/_form.html.twig' with {
    page_title: 'title.size_edit'|trans({'%id%': size.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'size_index',
} %}