{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.size_delete'|trans({'%id%': size.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.size_delete'|trans({'%id%': size.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('size_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}