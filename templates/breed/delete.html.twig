{% include 'default/_form.html.twig' with {
    page_title: 'title.breed_delete'|trans({'%id%': breed.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'breed_index',
} %}
