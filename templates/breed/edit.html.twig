{% include 'default/_form.html.twig' with {
    page_title: 'title.breed_edit'|trans({'%id%': breed.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'breed_index',
} %}