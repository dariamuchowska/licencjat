{% include 'default/_form.html.twig' with {
    page_title: 'title.gender_edit'|trans({'%id%': gender.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'gender_index',
} %}