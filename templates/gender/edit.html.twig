{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.gender_edit'|trans({'%id%': gender.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.gender_edit'|trans({'%id%': gender.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary"/>
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('gender_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}