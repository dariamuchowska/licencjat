{% include 'default/_form.html.twig' with {
    page_title: 'title.gender_delete'|trans({'%id%': gender.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'gender_index',
} %}
