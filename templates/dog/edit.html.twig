{% include 'default/_form.html.twig' with {
    page_title: 'title.dog_edit'|trans({'%id%': dog.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'dog_index',
} %}