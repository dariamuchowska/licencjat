{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.dog_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.dog_list'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'dog.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.name'|trans, 'dog.name') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.age'|trans, 'dog.age') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.description'|trans, 'dog.description') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.picture'|trans, 'dog.picture') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.sex'|trans, 'dog.sex') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.size'|trans, 'dog.size') }}</th>
                <th>{{ 'label.actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for dog in pagination.items %}
                <tr>
                    <td>{{ dog.id }}</td>
                    <td>{{ dog.name }}</td>
                    <td>{{ dog.age }}</td>
                    <td>{{ dog.description }}</td>
                    <td>{{ dog.picture }}</td>
                    <td>{{ dog.sex }}</td>
                    <td>{{ dog.size }}</td>
                    <td>
                        <a href="{{ url('dog_show', {id: dog.id}) }}" title="{{ 'action.show'|trans }}">
                            {{ 'action.show'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}