{% include 'default/_form.html.twig' with {
    page_title: 'title.dog_delete'|trans({'%id%': dog.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'dog_index',
} %}
